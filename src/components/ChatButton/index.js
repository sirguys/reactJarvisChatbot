import React, { Component } from 'react';
import './style.css';

class ChatButton extends Component {

  render() {
    // const { name, input, _handleChatButton } = this.props;

    // return <button className={'btn btn-primary' + (input.length ? '' : ' disabled')} onClick={_handleChatButton}>Send to {name}</button>
  };
}

// export default ChatButton;
